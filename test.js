import {
  api_usersession,
  creating_reward_api_testscript,
} from "./script/api/api-testscript";
import {
  redirect_login_for_unauthorized,
  creating_rewards,
  upload_bulk_list,
} from "./script/web/web-testscript";

// api_usersession();

// creating_reward_api_testscript();

// redirect_login_for_unauthorized();

// creating_rewards();

upload_bulk_list();
